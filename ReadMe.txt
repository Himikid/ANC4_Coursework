Documentation.pdf was the report submitted with this code.

To run example files simply copy and paste into the file named "network", currently
example2_network is copied into this file. Or edit the "network" file directly.

The program can also be directly run from Eclipse, import the project folder, you can append the "network" file inside this folder. Run the source file called "App" (found inside the package: network).


